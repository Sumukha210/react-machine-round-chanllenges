const transferData = ["first", "second", "third", "fourth"];

const TransferList = () => {
  return (
    <div className="grid place-items-center min-h-screen">
      <header>
        <h1 className="text-4xl font-bold">Transfer List</h1>
      </header>
    </div>
  );
};

export default TransferList;
